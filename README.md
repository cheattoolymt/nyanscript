# nyan< script - 完全パッケージ

**高速・安心安全・セキュリティ万全**

nyan< scriptは、JavaScriptを超える機能(いまのところはない)と安全性を持つ新しいプログラミング言語です。

##  特徴

-  **高速** - 最適化されたインタプリタ
-  **安心安全** - 型推論による安全性
-  **セキュリティ万全** - XSS/インジェクション対策組み込み
-  **簡単** - セミコロン不要、シンプルな構文
-  **ブラウザ対応** - HTMLに埋め込んで即実行
-  **自動検出** - 拡張機能の有無を自動判定

##  ファイル構成

```
nyanscript/
├── extension/                      # Chrome拡張機能
│   ├── manifest.json              # 拡張機能設定
│   ├── content-script.js          # メインスクリプト
│   └── nyanscript-interpreter.js  # インタプリタ
│
├── website/                        # Webサイト（デモ）
│   ├── index.html                 # デモページ
│   ├── nyanscript-detector.js     # 拡張機能検出スクリプト（重要！）
│   └── scripts/                   # .nysファイル群
│       ├── hello.nys
│       ├── conditional.nys
│       └── data-structures.nys
│
├── nyanscript-detector.js         # スタンドアロン版検出スクリプト
└── README.md                       # このファイル
```

##  重要なファイル

### `nyanscript-detector.js` - 拡張機能検出スクリプト
このファイルは**Webサーバーに配置**し、HTMLファイルで読み込むことで：
- ✅ 拡張機能の有無を自動検出
- ✅ 拡張機能がない場合、インストールを促すバナーを表示
- ✅ インストール手順を表示するモーダル機能

**全てのHTMLファイルに組み込むことを推奨します！**

##  クイックスタート

### ステップ1: Chrome拡張機能のインストール

1. `extension/` フォルダを確認
2. Chromeで `chrome://extensions/` を開く
3. 右上の「デベロッパーモード」をON
4. 「パッケージ化されていない拡張機能を読み込む」をクリック
5. `extension/` フォルダを選択

✅ 拡張機能がインストールされました！

### ステップ2: デモサイトを開く

#### 方法A: ローカルで開く
1. `website/index.html` をブラウザで直接開く
2. ページが表示され、nyan< scriptが自動実行される！

#### 方法B: Webサーバーで開く（推奨）
```bash
cd website
python -m http.server 8000
# または
npx serve
```
ブラウザで `http://localhost:8000` を開く

### ステップ3: 動作確認

- ページ上部に「拡張機能が必要です」バナーが**出ない**ことを確認
- 各デモセクションに🐱マークの出力が表示されることを確認

## 📖 使い方

### HTML内で直接実行

```html
<!DOCTYPE html>
<html>
<head>
    <title>My nyan< script Page</title>
    <!-- 拡張機能検出スクリプト（推奨） -->
    <script src="nyanscript-detector.js"></script>
</head>
<body>
    <h1>Hello!</h1>
    
    <!-- nyscriptタグで実行 -->
    <nyscript>
        console.outputx("Hello, World!")
    </nyscript>
    
</body>
</html>
```

### 外部ファイルを読み込む

**script.nys:**
```nys
#"これはnyan< scriptです"#
x = 10
y = 20
console.outputx("Result: " + (x + y))
```

**index.html:**
```html
<nyscript src="script.nys"></nyscript>
```

## 📖 言語仕様

### コメント
```nys
#"これはコメントです"#
```

### 変数（型推論自動）
```nys
x = 10              #"数値"#
name = "nyan"       #"文字列"#
arr = [1, 2, 3]     #"配列"#
obj = {key: "val"}  #"オブジェクト"#
```

### 関数定義
```nys
def greet(name) {
    console.outputx("Hello, " + name)
}
```

### 条件分岐
```nys
if x == 10 then {
    console.outputx("10です")
elif x == 20 then {
    console.outputx("20です")
else {
    console.outputx("それ以外")
}
```

### ループ
```nys
repeat i 5 {
    console.outputx("繰り返し: " + i + "\n")
}
```

### 配列・オブジェクトアクセス
```nys
arr = [1, 2, 3]
console.outputx(arr[0])  #"1"#

obj = {name: "nyan"}
console.outputx(obj.name)  #"nyan"#
```

### 組み込み関数
```nys
console.outputx("出力")              #"標準出力"#
console.inputx("入力してね", v)      #"入力受付"#
console.error("エラーメッセージ")    #"エラー出力"#
```

### モジュールインポート
```nys
$pull <module.nsm>
```

## 🎯 サンプルコード

### Hello World
```nys
console.outputx("Hello, World!")
```

### FizzBuzz
```nys
repeat i 15 {
    num = i + 1
    if num == 3 then {
        console.outputx("Fizz\n")
    elif num == 5 then {
        console.outputx("Buzz\n")
    elif num == 15 then {
        console.outputx("FizzBuzz\n")
    else {
        console.outputx(num + "\n")
    }
}
```

### 配列操作
```nys
numbers = [10, 20, 30, 40, 50]
sum = 0

repeat i 5 {
    sum = sum + numbers[i]
}

console.outputx("合計: " + sum)
```

## 🔧 開発ロードマップ

- [x] 基本構文（変数、関数、条件分岐、ループ）
- [x] Chrome拡張機能対応
- [x] 外部ファイル読み込み
- [ ] モジュールシステム（$pull）
- [ ] より高度な演算子（-、*、/、%など）
- [ ] for-inループ
- [ ] try-catch エラーハンドリング
- [ ] クラス・オブジェクト指向
- [ ] 非同期処理（async/await）
- [ ] Cインタプリタ実装
- [ ] WebAssemblyコンパイル

## 📄 ライセンス

このプロジェクトはオープンソースです。自由に使用・改変してください。

## 🤝 貢献

プルリクエスト、イシュー、フィードバック大歓迎です！

---

Made with 💖 by nyan< script team 🐱
